<h2>Your Cart:</h2>

<table class="table table-striped">
  <thead>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Qty</th>
  </tr>
  </thead>

        <% @items.each do |item| %>
  <tr>
        <td><%= item[0].title %></td>
        <td><%= item[0].prise %></td>
        <td><%= item[1] %></td> <!id  kol-vo>
  </tr>
        <%end%>
  </tbody>
  </table>